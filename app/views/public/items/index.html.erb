<div class="container">
  <div class="row">
    <div class="col-sm-12 px-sm-0">
      <h2>商品一覧(全<%= @items_all.count %>件)</h2>
    </div>
  </div>
  <div>
    <%= @items.each do |item| %>
      <%= link_to item_path(item) do %>
        <p><%= attachment_image_tag item, :image, width: "80%", format: "jpeg", fallback: "no_image.jpg" %></p>
      <% end %>
      <p>商品名 <%= item.name %></p>
      <% if item.is_active == true %>
        <p>¥<%= item.price,to_s(:dalimited) %></p>
      <% else %>
        <p>販売停止中</p>
      <% end %>
    <% end %>
  </div>
  <%= paginate @items, class: "text-center" %>
</div>
